plugins {
    id 'java-library'
}

dependencies {
    // Pure Java — no Minecraft or loader dependencies
    // Java 11+ HttpClient is used for REST API calls
}

jar {
    archiveBaseName = 'kmddns-common'
}
